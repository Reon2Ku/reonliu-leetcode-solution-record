# 123.买卖股票的最佳时机③（重做）

## 题目描述



给定一个数组，它的第 `i` 个元素是一支给定的股票在第 `i` 天的价格。

设计一个算法来计算你所能获取的最大利润。你最多可以完成 **两笔** 交易。

**注意：**你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

 

**示例 1:**

```
输入：prices = [3,3,5,0,0,3,1,4]
输出：6
解释：在第 4 天（股票价格 = 0）的时候买入，在第 6 天（股票价格 = 3）的时候卖出，这笔交易所能获得利润 = 3-0 = 3 。
     随后，在第 7 天（股票价格 = 1）的时候买入，在第 8 天 （股票价格 = 4）的时候卖出，这笔交易所能获得利润 = 4-1 = 3 。
```

**示例 2：**

```
输入：prices = [1,2,3,4,5]
输出：4
解释：在第 1 天（股票价格 = 1）的时候买入，在第 5 天 （股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。   
     注意你不能在第 1 天和第 2 天接连购买股票，之后再将它们卖出。   
     因为这样属于同时参与了多笔交易，你必须在再次购买前出售掉之前的股票。
```

**示例 3：**

```
输入：prices = [7,6,4,3,1] 
输出：0 
解释：在这个情况下, 没有交易完成, 所以最大利润为 0。
```

**示例 4：**

```
输入：prices = [1]
输出：0
```

 

**提示：**

- `1 <= prices.length <= 105`
- `0 <= prices[i] <= 105`



## 题解

这道题你刚开始做的时候，又陷入了做dp问题时经典的把局部最优当成整体最优的思维。尽管这样子做能通过80%的用例，但少数例外通过不了就是不行



这一题你最值得学习的点在于二维dp数组横轴和纵轴的含义，这一点是超出你目前认知的。以往，我们的一维dp数组和二维dp数组维度上的含义是一目了然的，但这个地方使用的二维数组：横轴理所当然的指的是第i天

纵轴指的是目前持有股票的状态，共5行：

0表示default

1表示第一次持有股票

2表示第一次不持有股票

3表示第二次持有股票

4表示第二次不持有股票

~~~
dp[i][j]的含义是：在第i天时，j状态下，自己的净收益，也就是说初始为0，买入后会变为负数，卖出后变为正数
~~~

![123.买卖股票的最佳时机III](https://file.kamacoder.com/pics/20201228181724295-20230310134201291.png)

如图



从这一点来看我们反思一下，如果这里的买卖次数从2变为k，那我们要设立多少行横轴？2k+1行，j天列。



从代码随想录目前已有的提示来看，我们艰难的写出了如下代码：

我草我草我草我真的写出来了，人生第一道看了思路后自己写出来的困难题耶耶耶耶耶耶耶

~~~
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        vector<vector<int>> dp(prices.size(),vector<int>(5,0));
        dp[0][1] = 0-prices[0];
        dp[0][3] = 0-prices[0];
        for(int j=1;j<=4;j++)
        {
            for(int i=1;i<prices.size();i++)
            {
                if(j%2==1)
                {
                    dp[i][j] = max(dp[i-1][j],dp[i-1][j-1]-prices[i]);
                    //cout<<"dp"<<i<<j<<"pass"<<dp[i][j]<<endl;
                }
                else if(j%2==0)
                {
                    dp[i][j] = max(dp[i-1][j],dp[i-1][j-1]+prices[i]);
                    //cout<<"dp"<<i<<j<<"pass"<<dp[i][j]<<endl;
                }
            }
        }

        return dp[prices.size()-1][4];
    }
};
~~~

