# 115.不同的子序列

## 题目描述

给你两个字符串 `s` 和 `t` ，统计并返回在 `s` 的 **子序列** 中 `t` 出现的个数，结果需要对 109 + 7 取模。

 

**示例 1：**

```
输入：s = "rabbbit", t = "rabbit"
输出：3
解释：
如下所示, 有 3 种可以从 s 中得到 "rabbit" 的方案。
rabbbit
rabbbit
rabbbit
```

**示例 2：**

```
输入：s = "babgbag", t = "bag"
输出：5
解释：
如下所示, 有 5 种可以从 s 中得到 "bag" 的方案。 
babgbag
babgbag
babgbag
babgbag
babgbag
```

 

**提示：**

- `1 <= s.length, t.length <= 1000`
- `s` 和 `t` 由英文字母组成



## 题解

md吊题目很难绷得住。

自己只想出来一半，实在没凑出来dp更新算式。但看了标准答案dp式之后又是随便ac，所以我才讨厌动规

~~~
class Solution {
public:
    int numDistinct(string s, string t) {
        vector<vector<unsigned long>> dp(s.size()+1,vector<unsigned long>(t.size()+1,0));
        for(int i=0;i<s.size()+1;i++) 
        {
            dp[i][0] = 1;
        }

        // for(int i=0;i<s.size()+1;i++)
        // {
        //     for(int j=0;j<t.size()+1;j++)
        //     {
        //         cout<<dp[i][j]<<"  ";
        //     }
        //     cout<<endl;
        // }

        for(int i=1;i<s.size()+1;i++)
        {
            for(int j=1;j<=i,j<t.size()+1;j++)
            {
                if(s[i-1]==t[j-1]) dp[i][j] = dp[i-1][j-1]+dp[i-1][j];
                else dp[i][j] = dp[i-1][j];
                //cout<<dp[i][j]<<endl;
            }
        }
        unsigned long result = dp[s.size()][t.size()];
        result = result % long(pow(10,9)+7);
        return result;
    }
};
~~~

