# 707.设计链表

## 题目描述

你可以选择使用单链表或者双链表，设计并实现自己的链表。

单链表中的节点应该具备两个属性：`val` 和 `next` 。`val` 是当前节点的值，`next` 是指向下一个节点的指针/引用。

如果是双向链表，则还需要属性 `prev` 以指示链表中的上一个节点。假设链表中的所有节点下标从 **0** 开始。

实现 `MyLinkedList` 类：

- `MyLinkedList()` 初始化 `MyLinkedList` 对象。
- `int get(int index)` 获取链表中下标为 `index` 的节点的值。如果下标无效，则返回 `-1` 。
- `void addAtHead(int val)` 将一个值为 `val` 的节点插入到链表中第一个元素之前。在插入完成后，新节点会成为链表的第一个节点。
- `void addAtTail(int val)` 将一个值为 `val` 的节点追加到链表中作为链表的最后一个元素。
- `void addAtIndex(int index, int val)` 将一个值为 `val` 的节点插入到链表中下标为 `index` 的节点之前。如果 `index` 等于链表的长度，那么该节点会被追加到链表的末尾。如果 `index` 比长度更大，该节点将 **不会插入** 到链表中。
- `void deleteAtIndex(int index)` 如果下标有效，则删除链表中下标为 `index` 的节点。

 

**示例：**

```
输入
["MyLinkedList", "addAtHead", "addAtTail", "addAtIndex", "get", "deleteAtIndex", "get"]
[[], [1], [3], [1, 2], [1], [1], [1]]
输出
[null, null, null, null, 2, null, 3]

解释
MyLinkedList myLinkedList = new MyLinkedList();
myLinkedList.addAtHead(1);
myLinkedList.addAtTail(3);
myLinkedList.addAtIndex(1, 2);    // 链表变为 1->2->3
myLinkedList.get(1);              // 返回 2
myLinkedList.deleteAtIndex(1);    // 现在，链表变为 1->3
myLinkedList.get(1);              // 返回 3
```



## 题解

我日写的要了我的老命了

一开始想着随便写写吧没有想到思维量那么大，尤其是好几个函数堆在一起都要考虑

光用leetcode来debug太低效了，到了后期chatgpt也帮不了你很大忙，只能在刚开始代码乱七八糟的时候给你一些非常泛泛而谈的建议

真的debug还得自己打开visual studio并且写个print函数用来测试

以下是自己拼了老命写的代码

理解不透彻就不要随便delete了，被你delete的乱七八糟的。不delete之后就正常跑起来了

`````
class MyLinkedList {
public:
    struct ListNode{
    int val;
    ListNode* next;
    ListNode(int v):val(v),next(nullptr){}
    };//leetcode网站上预定义了ListNode，这里建议你换个名字，比如LinkedNode之类的以作区分
    //不然就只可以像这样把结构体的定义写在类内才不会出现重名冲突

    MyLinkedList() {
        dummy_head = new ListNode(0);
        size = 0;
    }
    
    int get(int index) {    
        if(index>=size || index<0) return -1;//不要忽略小于0的情况
        ListNode *curr = dummy_head->next;
        for(int i=0;i<index;i++)
        {
            curr = curr->next;
        }
        return curr->val;
    }
    
    void addAtHead(int val) {
        ListNode *temp = new ListNode(val);
        temp->next = dummy_head->next;
        dummy_head->next = temp;
        size++;
    }
    
    void addAtTail(int val) {
        ListNode *curr = dummy_head;
        for(int i=0;i<size;i++)
        {
            curr = curr->next;
        }
        curr->next = new ListNode(val);
        size++;
    }
    
    void addAtIndex(int index, int val) {
        if(index>size || index<0) return;//注意，add时的index可以等于size，相当于尾加，我找了半天的问题就在这一行
        ListNode *curr = dummy_head;
        for(int i=0;i<index;i++)
        {
            curr = curr->next;
        }
        ListNode *temp = curr->next;
        curr->next = new ListNode(val);
        curr->next->next = temp;
        size++;
    }
    
    void deleteAtIndex(int index) {
        if(index>=size || index<0) return;//当然，delete时index就只可以小于size了
        ListNode *curr = dummy_head;
        for(int i=0;i<index;i++)
        {
            curr = curr->next;
        }
        ListNode *temp = curr->next;
        curr->next = curr->next->next;
        size--;
    }

    void printLinkedList() {
        ListNode* curr = dummy_head;
        while (curr->next != nullptr) {
            cout << curr->next->val << " ";
            curr = curr->next;
        }
        cout << endl;
    }

private://注意书写习惯，成员变量写成私有的
    int size;
    ListNode *dummy_head;//在写链表时，引入一个虚拟头会更方便后续操作
};

/**
 * Your MyLinkedList object will be instantiated and called as such:
 * MyLinkedList* obj = new MyLinkedList();
 * int param_1 = obj->get(index);
 * obj->addAtHead(val);
 * obj->addAtTail(val);
 * obj->addAtIndex(index,val);
 * obj->deleteAtIndex(index);
 */
`````

