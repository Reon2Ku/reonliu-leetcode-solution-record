# 342. Power of Four

## question

Given an integer `n`, return *`true` if it is a power of four. Otherwise, return `false`*.

An integer `n` is a power of four, if there exists an integer `x` such that `n == 4x`.

 

**Example 1:**

```
Input: n = 16
Output: true
```

**Example 2:**

```
Input: n = 5
Output: false
```

**Example 3:**

```
Input: n = 1
Output: true
```

 

**Constraints:**

- `-231 <= n <= 231 - 1`

 

**Follow up:** Could you solve it without loops/recursion?



## solution

~~~
class Solution {
public:
    bool isPowerOfFour(int n) {
        if(n==0) return false;
        while(n%4==0) n/=4;
        return n==1;
    }
};
~~~

要说有什么好方法也就只有打表了。剩下的就是数学规律，但数学规律背不了也想不出。

~~~
class Solution {
public:
    bool isPowerOfFour(int n) {
        set<int> myset = {1,
                        4,
                        16,
                        64,
                        256,
                        1024,
                        4096,
                        16384,
                        65536,
                        262144,
                        1048576,
                        4194304,
                        16777216,
                        67108864,
                        268435456,
                        1073741824};
        // n=1;
        // while(n<pow(2,30))
        // {
        //     cout<<n<<endl;
        //     n*=4;
        // }
        // cout<<n;

        return myset.find(n)!=myset.end();
    }
};
~~~

写一下数学解吧，不过我懒得学

~~~
return (n & ((long long)n - 1)) == 0 && n % 3 == 1;
~~~

