# 516.最长回文子序列

## 题目描述

给你一个字符串 `s` ，找出其中最长的回文子序列，并返回该序列的长度。

子序列定义为：不改变剩余字符顺序的情况下，删除某些字符或者不删除任何字符形成的一个序列。

 

**示例 1：**

```
输入：s = "bbbab"
输出：4
解释：一个可能的最长回文子序列为 "bbbb" 。
```

**示例 2：**

```
输入：s = "cbbd"
输出：2
解释：一个可能的最长回文子序列为 "bb" 。
```

 

**提示：**

- `1 <= s.length <= 1000`
- `s` 仅由小写英文字母组成



## 题解

自己写的，还不错

~~~
class Solution {
public:
    int longestPalindromeSubseq(string s) {
        int result = 1;
        vector<vector<int>> isPali(s.size(),vector<int>(s.size(),0));
        for(int i=s.size()-1;i>=0;i--)
        {
            for(int j=i;j<s.size();j++)
            {
                if(s[i]==s[j])
                {
                    if(j-i>1)
                    {
                        isPali[i][j] = isPali[i+1][j-1] + 2;
                        result = max(result,isPali[i][j]);
                    }
                    else if(j-i<=1)
                    {
                        isPali[i][j] = j-i+1;
                        result = max(isPali[i][j],result);
                    }
                }
                else
                {
                    isPali[i][j] = max(isPali[i][j-1],isPali[i+1][j]);
                }
            }
        }
        // for(int i=0;i<s.size();i++)
        // {
        //     if(s[i]=='g') 
        //     {
        //         cout<<i<<" "<<s.size()-i<<endl;
        //         break;
        //     }
        // }
        return result;
    }
};
~~~

简化一下代码后：

~~~
class Solution {
public:
    int longestPalindromeSubseq(string s) {
        vector<vector<int>> isPali(s.size(),vector<int>(s.size(),0));
        for(int i=s.size()-1;i>=0;i--)
        {
            for(int j=i;j<s.size();j++)
            {
                if(s[i]==s[j])
                {
                    if(j-i>1)
                    {
                        isPali[i][j] = isPali[i+1][j-1] + 2;
                    }
                    else if(j-i<=1)
                    {
                        isPali[i][j] = j-i+1;
                    }
                }
                else
                {
                    isPali[i][j] = max(isPali[i][j-1],isPali[i+1][j]);
                }
            }
        }
        // for(int i=0;i<s.size();i++)
        // {
        //     if(s[i]=='g') 
        //     {
        //         cout<<i<<" "<<s.size()-i<<endl;
        //         break;
        //     }
        // }
        return isPali[0][s.size()-1];
    }
};
~~~

