# 3.无重复字符的最长子串

## 题目描述

给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。

 

**示例 1:**

```
输入: s = "abcabcbb"
输出: 3 
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
```

**示例 2:**

```
输入: s = "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。
```

**示例 3:**

```
输入: s = "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。
```

 

**提示：**

- `0 <= s.length <= 5 * 104`
- `s` 由英文字母、数字、符号和空格组成



## 题解

欸？我不是天才吗？

4ms击败86.5%，,10.74MB击败93.32%

~~~
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        if(s.size()==0) return 0;
        //int endIndex = 0;结束索引就用i
        int startIndex = 0;
        int result = 0;
        vector<int> index(100,-1);
        //cout<<s[0]-'a'<<endl;
        for(int i=0;i<s.size();i++)
        {
            if(index[s[i]-' ']==-1) 
            {
                index[s[i]-' '] = i;
                result = max(result, i-startIndex+1);
                //cout<<result<<endl;
            }
            else
            {
                startIndex = max(index[s[i]-' ']+1,startIndex);
                index[s[i]-' ']=i;
                result = max(result, i-startIndex+1);
                //cout<<result<<endl;
            }

        }
        return result;
    }
};
~~~

